# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: genai-rag-ingress
#   annotations:
#     nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
# spec:
#   rules:
#     - host: YOUR_DOMAIN
#       http:
#         paths:
#           - path: /
#             pathType: Prefix
#             backend:
#               service:
#                 name: genai-rag-service
#                 port:
#                   number: 80


apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: healthcare-ingress
  annotations:
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/cors-allow-origin: "*"
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx
  rules:
    - host: healthai.duckdns.org  # Your existing domain
      http:
        paths:
          # Frontend - serve React app at root
          - path: /()(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: healthcare-frontend-service
                port:
                  number: 80
          
          # Backend API - all /api/* routes
          - path: /api(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: healthcare-rag-service
                port:
                  number: 80